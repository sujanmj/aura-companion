# AURA Companion dependencies
# Phase 0 core features use mostly the Python standard library.
# OpenCV 4.x required for Haar cascade face detection (5.x removed CascadeClassifier).
opencv-python>=4.8.0,<5
# Future local STT:
# faster-whisper
# sounddevice
# numpy
